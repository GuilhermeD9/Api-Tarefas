version: '3.1'
services:
  mysql:
    image: mysql:latest
    container_name: todolist
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: todolist
      MYSQL_USER: dolphin
      MYSQL_PASSWORD: admin
    ports:
      - "3306:3306"
    restart: always